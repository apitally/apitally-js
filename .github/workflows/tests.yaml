name: Tests
on:
  push:
    branches:
      - main
      - v1
    paths-ignore:
      - .gitignore
      - LICENSE
      - README.md
  pull_request:
  schedule:
    - cron: "0 0,12 * * *"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: "24"
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run checks
        run: npm run check
      - name: Build package
        run: npm run build
      - name: Check package types
        run: npm run check:package

  test-coverage:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: "24"
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run tests with coverage
        run: npm run test:coverage
      - name: Upload coverage report to Codecov
        uses: codecov/codecov-action@57e3a136b779b570ffcdbf80b3bdc90e7fab3de2 # v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          directory: ./coverage/

  test-matrix:
    name: test-matrix (${{ matrix.node }}, ${{ matrix.scenario }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        node: ["20", "22", "24"]
        scenario:
          - express
          - express-5.0
          - express-4.18
          - fastify
          - fastify-4
          - fastify-4.10
          - hono
          - hono-4.8
          - nestjs-10
          - nestjs-11
        include:
          - scenario: express
            packages: express
          - scenario: express-5.0
            packages: express@5.0.0 pino@9.6.0
          - scenario: express-4.18
            packages: express@4.18.2 winston@3.2.0 @sentry/node@7
          - scenario: fastify
            packages: fastify
          - scenario: fastify-4
            packages: fastify@4
          - scenario: fastify-4.10
            packages: fastify@4.10.2
          - scenario: hono
            packages: hono
          - scenario: hono-4.8
            packages: hono@4.8.4 @opentelemetry/api@1.9.0
          - scenario: nestjs-10
            packages: "@nestjs/common@10 @nestjs/core@10 @nestjs/platform-express@10 @nestjs/platform-fastify@10"
          - scenario: nestjs-11
            packages: "@nestjs/common@11 @nestjs/core@11 @nestjs/platform-express@11 @nestjs/platform-fastify@11"
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: "${{ matrix.node }}"
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Install scenario dependencies
        run: npm install --no-save --force ${{ matrix.packages }}
      - name: Run tests
        run: npm test

  ci-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs:
      - check
      - test-coverage
      - test-matrix
    if: always()
    steps:
      - name: Fail if any job failed or was cancelled
        if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')
        run: exit 1
